title: "Threshold Church"
description: "Sundays at 9 & 11am on Riverside, 11am in East Austin. A church for people who thought church wasn't for them."

# The template's own assets are copied through untouched.
include: ['church.config.json']
exclude: ['README.md', 'LICENSE', 'Gemfile', 'Gemfile.lock', 'vendor']
sass:
  style: compressed
